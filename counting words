/*Write a Java program to display count of all words in a given string to a function. eg.input string : This is Coding Class. output : 4*/
public class CountWords
{
    public static void main(String[] args) 
{
      String str = "This Is Coding Class";
 
        int count = 1;
 
        for (int i = 0; i < str.length() - 1; i++)
        {
            if ((str.charAt(i) == ' ') && (str.charAt(i + 1) != ' '))
     {
                count++;
    } }
        System.out.println("Number of words in a string " + count);
    }}
