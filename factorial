/*Write a java program to find factorial of n by implementing the concept of nesting of functions. - call display() method which calls fact() method. */
import java.util.*;
class Factorial
{
public static void main(String args[])
{
Scanner scan=new Scanner(System.in);
System.out.println("Enter a number");
int num=scan.nextInt();
System.out.print("Factorial of "+num+"is "+factorial(num));
}
static int factorial(int num)
{
int i,fact=1;
for(i=1; i<=num; i++)
{
fact=fact*i;
}
return fact;
}
}
